<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Aegis AI is a proactive productivity companion that prioritizes tasks, auto-blocks calendar schedules, tracks habits, and features customized deep work environments with synthesized sound waves." />
  <title>Aegis AI - Time Blocking Grid</title>
  <link rel="stylesheet" href="/css/index.css">
  <link rel="stylesheet" href="/css/app.css">
  <script src="/js/app.js" defer></script>
  <script src="/js/calendar.js" defer></script>
</head>
<body>
  <div class="app-container">
    <div class="ambient-glow-1"></div>
    <div class="ambient-glow-2"></div>

    <!-- Sidebar Navigation -->
    <aside class="sidebar">
      <div>
        <% const username = userEmail ? userEmail.split('@')[0] : 'guest' %>
        <a href="/<%= username %>/index" class="logo-section">
          <div class="logo-icon">A</div>
          <span class="logo-text">Aegis AI</span>
        </a>
        <nav>
          <ul class="nav-links">
            <li><a href="/<%= username %>/index" class="nav-item">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="7" height="9" x="3" y="3" rx="1" /><rect width="7" height="5" x="14" y="3" rx="1" /><rect width="7" height="9" x="14" y="10" rx="1" /><rect width="7" height="5" x="3" y="16" rx="1" /></svg>
              <span>Dashboard</span>
            </a></li>
            <li><a href="/<%= username %>/calendar" class="nav-item active">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 2v4" /><path d="M16 2v4" /><rect width="18" height="18" x="3" y="4" rx="2" /><path d="M3 10h18" /></svg>
              <span>Time Blocking</span>
            </a></li>
            <li><a href="/<%= username %>/deepwork" class="nav-item">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10" /><polygon points="10 8 16 12 10 16 10 8" /></svg>
              <span>Deep Work</span>
            </a></li>
            <li><a href="/<%= username %>/habits" class="nav-item">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22c5.523 0 10-4.477 10-10S17.523 2 12 2 2 6.477 2 12s4.477 10 10 10z" /><path d="m9 12 2 2 4-4" /></svg>
              <span>Habit Tracker</span>
            </a></li>
          </ul>
        </nav>
      </div>
      <div class="sidebar-footer">
        <div class="user-profile">
          <div class="avatar"><%= userEmail ? userEmail.split('@')[0].charAt(0).toUpperCase() : 'U' %></div>
          <div class="user-info">
            <p><%= userEmail ? userEmail.split('@')[0] : 'Guest User' %></p>
            <span>Focus Mode Active</span>
          </div>
        </div>
      </div>
    </aside>

    <!-- Main Content Area -->
    <main class="content-area">
      <header class="app-header">
        <div class="header-title-section">
          <h1>Time Blocking Grid</h1>
          <p>Schedule your day proactively to secure focused execution time</p>
        </div>
        <div class="header-status-section" style="gap: 0.5rem;">
          <button class="btn btn-danger" id="clear-schedule-btn">
            Clear Auto-Blocks
          </button>
          <button class="btn btn-primary" id="optimize-schedule-btn">
            🪄 AI Auto-Schedule
          </button>
        </div>
      </header>

      <div class="dashboard-grid">
        <!-- Left Column: Hourly Grid -->
        <div class="dashboard-left" style="flex: 1.4;">
          <div class="glass-card calendar-view">
            <div class="calendar-grid">
              <div class="calendar-header">
                <div>Time</div>
                <div>Focus Schedule</div>
              </div>
              <div class="calendar-body" id="calendar-grid-body">
                <!-- Dynamically populated time slot rows -->
              </div>
            </div>
          </div>
        </div>

        <!-- Right Column: Month Calendar & Unscheduled Tasks -->
        <div class="dashboard-right" style="display: flex; flex-direction: column; gap: 1rem;">
          <!-- Month Navigation Calendar Card -->
          <div class="glass-card month-calendar-card" style="display: flex; flex-direction: column; gap: 0.75rem;">
            <div style="display: flex; align-items: center; justify-content: space-between;">
              <h3 class="card-title" id="month-year-label" style="font-size: 1rem; margin: 0; text-transform: capitalize;">Month Year</h3>
              <div style="display: flex; gap: 0.25rem;">
                <button type="button" class="btn" id="prev-month-btn" style="padding: 0.2rem 0.5rem; font-size: 0.75rem;">◀</button>
                <button type="button" class="btn" id="next-month-btn" style="padding: 0.2rem 0.5rem; font-size: 0.75rem;">▶</button>
              </div>
            </div>
            <div style="display: grid; grid-template-columns: repeat(7, 1fr); text-align: center; font-size: 0.7rem; color: var(--text-secondary); border-bottom: 1px solid var(--border-light); padding-bottom: 0.25rem;">
              <div>Su</div><div>Mo</div><div>Tu</div><div>We</div><div>Th</div><div>Fr</div><div>Sa</div>
            </div>
            <div id="month-days-grid" style="display: grid; grid-template-columns: repeat(7, 1fr); gap: 0.25rem; font-size: 0.75rem;">
              <!-- Populated dynamically -->
            </div>
          </div>

          <div class="glass-card" style="display: flex; flex-direction: column; gap: 1rem;">
            <h3 class="card-title">Unscheduled Tasks</h3>
            
            <div id="unscheduled-task-list" style="display: flex; flex-direction: column; gap: 0.5rem; max-height: 250px; overflow-y: auto;">
              <!-- Dynamically populated unscheduled tasks -->
            </div>

            <!-- Manual Scheduling Form (revealed on click) -->
            <form id="manual-schedule-form" style="display: none; border-top: 1px solid var(--border-light); padding-top: 1rem; flex-direction: column; gap: 0.75rem;">
              <h4 style="font-size: 0.85rem; font-weight: 600;" id="manual-form-title">Schedule Selected Task</h4>
              
              <div class="form-group">
                <label for="manual-time-select">Start Time</label>
                <select class="form-input" id="manual-time-select">
                  <!-- Dynamically populated slots -->
                </select>
              </div>

              <div style="display: flex; gap: 0.5rem;">
                <button type="button" class="btn" style="flex: 1; justify-content: center;" id="manual-form-cancel-btn">
                  Cancel
                </button>
                <button type="submit" class="btn btn-primary" style="flex: 1; justify-content: center;">
                  Add Block
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </main>

    <!-- Floating AI voice assistant overlay and side-panel -->
    <button class="ai-toggle-floating" id="ai-toggle-btn" title="Ask Aegis AI">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#000" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z" />
        <path d="M19 10v2a7 7 0 0 1-14 0v-2" />
        <line x1="12" x2="12" y1="19" y2="22" />
      </svg>
    </button>

    <div class="ai-assistant-panel" id="ai-panel">
      <div class="ai-header">
        <div class="ai-header-title">
          <span class="pulse-indicator"></span>
          <span>Aegis Assistant</span>
        </div>
        <button style="background: transparent; border: none; cursor: pointer; font-size: 1.2rem; color: var(--text-secondary);" id="ai-panel-close-btn">
          ✕
        </button>
      </div>

      <div class="ai-body" id="ai-messages-container">
        <!-- AI chats go here -->
      </div>

      <div class="ai-footer">
        <button class="mic-btn" id="ai-mic-btn" title="Speak a command">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z" />
            <path d="M19 10v2a7 7 0 0 1-14 0v-2" />
            <line x1="12" x2="12" y1="19" y2="22" />
          </svg>
        </button>
        <form class="chat-input-wrapper" id="ai-chat-form">
          <input
            type="text"
            id="ai-chat-input"
            class="chat-input"
            placeholder="e.g. Schedule my tasks today..."
          />
          <button type="submit" class="send-btn">➔</button>
        </form>
      </div>
    </div>

    <!-- Toast Notification Container -->
    <div class="toast-container" id="toast-container"></div>
  </div>
</body>
</html>
